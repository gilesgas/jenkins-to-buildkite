{
  "name": "jenkins-to-buildkite-app",
  "version": "0.1.0",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "lint": "npx eslint",
    "test": "vitest run --coverage.enabled --coverage.reporter=html",
    "build": "mkdir -p dist && cp index.html index.js dist/"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.4",
    "@vitest/eslint-plugin": "^1.3.3",
    "buildkite-test-collector": "^1.9.0",
    "eslint": "^9.30.0",
    "vitest": "^3.1.3"
  }
}
