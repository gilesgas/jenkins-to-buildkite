{
  "name": "jenkins-to-buildkite",
  "version": "0.1.0",
  "type": "module",
  "main": "index.js",
  "dependencies": {
    "@vitest/coverage-v8": "^3.2.4",
    "vitest": "^3.1.3"
  },
  "scripts": {
    "lint": "echo 'Sweet, no issues!'",
    "test:unit": "vitest run --coverage.enabled --coverage.reporter=html",
    "test:integration": "vitest run",
    "build": "echo 'Build completed successfully!'"
  }
}
